# CTG Core — Corporate Deployment Stack
# Version: 1.1.0
# Architecture: Option B (independent tenant stack + gatekeeper)
#
# Services: PostgreSQL, Paperclip, OpenClaw, Mission Control, Gatekeeper, n8n
# Networks: internal-net (isolated), gateway-net (internet access)
# Ports: Offset +10000 from container ports

services:
  # ---------- Database ----------
  postgresql:
    image: postgres:16-alpine
    container_name: ctg-core-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${PG_USER:-ctgcore}
      POSTGRES_PASSWORD: ${PG_PASSWORD:?Set PG_PASSWORD in .env}
      POSTGRES_DB: ${PG_DB:-ctgcore}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - internal-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PG_USER:-ctgcore}"]
      interval: 10s
      timeout: 5s
      retries: 5
    # user: "999:999"  # removed — let postgres use its default internal user
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ---------- Paperclip (Task Management + Agent Registry) ----------
  paperclip:
    image: reeoss/paperclipai-paperclip:latest
    container_name: ctg-core-paperclip
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: "3100"
      HOST: "0.0.0.0"
      DATABASE_URL: "postgresql://${PG_USER:-ctgcore}:${PG_PASSWORD}@postgresql:5432/${PG_DB:-ctgcore}"
      SERVE_UI: "true"
      DEPLOYMENT_MODE: "local_trusted"
    ports:
      - "${PAPERCLIP_PORT:-13100}:3100"
    depends_on:
      postgresql:
        condition: service_healthy
    networks:
      - internal-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3100/api/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
    user: "1000:1000"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ---------- OpenClaw (Gateway + Agents) ----------
  openclaw:
    image: ghcr.io/ccubillas21/ctg-openclaw:${CTG_VERSION:-latest}
    container_name: ctg-core-openclaw
    restart: unless-stopped
    environment:
      NODE_ENV: production
      OPENCLAW_PORT: "18789"
      OPENCLAW_BIND: "0.0.0.0"
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:?Set ANTHROPIC_API_KEY in .env}
      OPENCLAW_AUTH_TOKEN: ${OPENCLAW_AUTH_TOKEN}
      PAPERCLIP_API_URL: "http://paperclip:3100"
      PAPERCLIP_API_KEY: ${PAPERCLIP_API_KEY:-}
      COMPANY_ID: ${COMPANY_ID}
      PARENT_HUB_URL: ${PARENT_HUB_URL:-}
      PARENT_HUB_TOKEN: ${PARENT_HUB_TOKEN:-}
      GATEKEEPER_INTERNAL_TOKEN: ${GATEKEEPER_INTERNAL_TOKEN}
    ports:
      - "${GW_PORT:-28789}:18789"
    volumes:
      - openclaw-data:/home/ctg/.openclaw/workspace
      - openclaw-config:/home/ctg/.openclaw
    depends_on:
      paperclip:
        condition: service_healthy
    networks:
      - internal-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:18789/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
    user: "1000:1000"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ---------- Mission Control (Dashboard) ----------
  mission-control:
    image: node:22-alpine
    container_name: ctg-core-mc
    restart: unless-stopped
    working_dir: /app
    command: ["npx", "@openclaw/mission-control", "--port", "4000"]
    environment:
      NODE_ENV: production
      PORT: "4000"
      PAPERCLIP_URL: "http://paperclip:3100"
    ports:
      - "${MC_PORT:-14000}:4000"
    depends_on:
      paperclip:
        condition: service_healthy
    networks:
      - internal-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:4000/api/status || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
    user: "1000:1000"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ---------- Gatekeeper (Egress Control + Hub Relay) ----------
  gatekeeper:
    image: ghcr.io/ccubillas21/ctg-gatekeeper:${CTG_VERSION:-latest}
    container_name: ctg-core-gatekeeper
    restart: unless-stopped
    environment:
      GATEKEEPER_PORT: ${GATEKEEPER_PORT:-9090}
      GATEKEEPER_INTERNAL_TOKEN: ${GATEKEEPER_INTERNAL_TOKEN}
      GATEKEEPER_DB_PATH: /data/gatekeeper.db
      GATEKEEPER_PRICING_PATH: /data/pricing.json
      GATEKEEPER_DOMAIN_ALLOWLIST: ${GATEKEEPER_DOMAIN_ALLOWLIST:-}
      LICENSE_GRACE_HOURS: ${LICENSE_GRACE_HOURS:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      COMPANY_ID: ${COMPANY_ID}
      PARENT_HUB_URL: ${PARENT_HUB_URL:-}
      PARENT_HUB_TOKEN: ${PARENT_HUB_TOKEN:-}
      CHECKIN_INTERVAL_MS: ${CHECKIN_INTERVAL_MS:-300000}
      OPENCLAW_URL: "http://openclaw:18789"
      PAPERCLIP_URL: "http://paperclip:3100"
      N8N_URL: "http://n8n:5678"
    ports:
      - "${GATEKEEPER_HOST_PORT:-19090}:9090"
    volumes:
      - gatekeeper-data:/data
    depends_on:
      postgresql:
        condition: service_healthy
    networks:
      - internal-net
      - gateway-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9090/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    user: "1000:1000"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # ---------- n8n (Workflow Automation) ----------
  n8n:
    image: n8nio/n8n:1.76.1
    container_name: ctg-core-n8n
    restart: unless-stopped
    environment:
      N8N_BASIC_AUTH_ACTIVE: "true"
      N8N_BASIC_AUTH_USER: ${N8N_BASIC_AUTH_USER}
      N8N_BASIC_AUTH_PASSWORD: ${N8N_BASIC_AUTH_PASSWORD}
      N8N_ENCRYPTION_KEY: ${N8N_ENCRYPTION_KEY}
      WEBHOOK_URL: "http://localhost:${N8N_PORT:-5678}/"
    ports:
      - "${N8N_PORT:-5678}:5678"
    volumes:
      - n8n-data:/home/node/.n8n
    networks:
      - internal-net
      - gateway-net
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:5678/healthz || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    user: "1000:1000"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  pgdata:
    driver: local
  openclaw-data:
    driver: local
  openclaw-config:
    driver: local
  gatekeeper-data:
    driver: local
  n8n-data:
    driver: local

networks:
  internal-net:
    driver: bridge
    internal: true
  gateway-net:
    driver: bridge
